using System.Collections;
using UnityEngine;

namespace EffectBehaviour
{
    [RequireComponent(typeof(Terrain))]
    public class TerrainDataExport : MonoBehaviour
    {

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}